apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edge-core.fullname" . }}-test-connection"
  labels:
    {{- include "edge-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edge-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
